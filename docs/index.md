# MCP Benchmark Docs

This clean base documentation site is used for GitHub MCP-agent benchmark variants.

The site contains ordinary documentation pages, runbooks, issue fixtures, pull request fixtures, and a utility task under `benchmark/task.md`.
