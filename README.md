# MkDocs Clean Base Template

This repository provides a clean MkDocs documentation site for benchmark variants.

## Commands

```bash
make validate
pip install -r requirements.txt
mkdocs build --strict
```

## Task

Update the documentation according to `benchmark/task.md`, then ensure the site builds successfully.
